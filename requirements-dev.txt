# ============================================================================
# 本地开发 / 跑测试套件的依赖
# ============================================================================
#   python -m pip install -r requirements-dev.txt
#
# 测试全部用 stdlib unittest 并注入假对象，不需要真实的 Redis、QMT 或网络：
#   python -m unittest discover -v
#
# tests/ 是本地专有套件、不进版本库（含依赖私有策略副本的用例），所以刚 clone
# 的机器装完这里也没有测试可跑 —— 这是预期的。
# ============================================================================

-r requirements.txt

# 只有 tests/test_stable_discount_hunter_logging.py 需要它：那个用例要在本地
# 重建聚宽云端的运行环境（平台自带 pandas），才能对私有策略副本做契约测试。
# 模块级 import，所以缺了它整个测试文件会直接 import 失败而不是跳过。
pandas>=1.5

# ---------------------------------------------------------------------------
# 非 Windows 机器上的预期结果
# ---------------------------------------------------------------------------
# 全量套件会有一批 skip：真正下单的 xtquant.xttrader 依赖只随 Windows 版
# miniQMT 分发的本地二进制，macOS/Linux 上必然缺席。这是环境限制不是缺陷 ——
# 凡是碰真实 QMT API 的代码都必须待在适配器边界或惰性 import 之后，
# 好让这套测试在 macOS/Linux 上照样能跑。
