__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/

# macOS / editor metadata
.DS_Store
**/.DS_Store

# Local secrets and environment overrides
.env
.env.*
!.env.example
secrets.*

# 实盘配置只留模板。真实的 host / 端口 / 账号 / QMT 路径 / 凭据一律不进版本库；
# 每台交易机在本地从 config.example.yaml 复制一份改名即可（如 config.gj.yaml）。
# 也包括 QMT 本地策略参数 config.strategy.yaml；真实策略阈值不进入版本库。
config*.yaml
config*.json
!config.example.yaml
!config.strategy.example.yaml

# Runtime state
data/
*.db
*.sqlite
*.sqlite3
*.log
logs/

.claude/
# 本地 agent 工作流状态（task 报告 / review diff），不进入版本库
.superpowers/
strategies/
CLAUDE.md
# Redis 服务器部署方式与环境强相关，仅保留本地维护。
deploy/
# 本地测试套件（含私有策略相关用例），不进入版本库
tests/

# 交易机本地手动实盘信号脚本；参数与 signal_id 不进版本库。
/send_manual_trade_signal.py

# 链路图产物(含私有规则时点, 仅本机)
docs/diagrams/

docs/

tools/